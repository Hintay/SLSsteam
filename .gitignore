.clangd
.git
bin
obj
zip

# Lua 5.4 sources are fetched at build time, not committed.
/third_party/lua/

# Developer notes live in their own repo (docs/developer/); docs/superpowers/ is
# the superpowers working dir. Neither is part of this repo — only docs/LICENSE/ is.
docs/developer/
docs/superpowers/
.DS_Store

# protobuf is fetched + built + generated at build time (see Makefile), not committed.
/third_party/protobuf/
/obj/proto/
/tools/protoc
/lib/libprotobuf-lite.a
